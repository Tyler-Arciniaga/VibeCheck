import { Text, View, Button } from "react-native";

export default function SignInScreen() {
    return (
        <View style={{
            flex: 1,
            justifyContent: "center",
            alignItems: "center",
          }}
        >
            <Text>This is the sign in screen for when users already have made accounts</Text>

        
        </View>
    );
}